[project]
name = "attacker-script"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Hughmann01134", email = "156956163+HughMann01134@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
]

[project.scripts]
attacker-script = "attacker_script:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
